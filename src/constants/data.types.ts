export interface IJob {
    name: string;
    location: string;
    responsibilities: string[];
    moreInfo: string;
    quickApply: string;
}

export interface IStatistics {
    label: string;
    value: number;
}
